---
meraki_api_key: "{{ meraki_key }}"
org_id: "{{ org_id }}"

SSID:
  - name: Cape_town2
    number: 1
    enabled: true
    authentication:
      auth_mode: psk
      encryption_mode: wpa
      psk: 
      wpa_encryption_mode: WPA2 only
    visible: false
    availableOnAllAps: true
    
  - name: Johannesburg
    number: 2
    enabled: true
    authentication:
      auth_mode: psk
      encryption_mode: wpa
      psk: 
      wpa_encryption_mode: WPA2 only
    state: present
    visible: true
    availableOnAllAps: true
    visible: true
    availableOnAllAps: true
    
  - name: Guest-CPT
    number: 3
    enabled: false
    authentication:
      auth_mode: psk
      encryption_mode: wpa
      psk: 
      wpa_encryption_mode: WPA2 only
    state: present
    visible: false
    availableOnAllAps: true
    
  - name: Guest-JHB
    number: 4
    enabled: true
    authentication:
      auth_mode: psk
      encryption_mode: wpa
      psk: 
      wpa_encryption_mode: WPA2 only
    state: present
    visible: false
    availableOnAllAps: true
